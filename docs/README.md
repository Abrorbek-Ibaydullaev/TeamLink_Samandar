# TaskLink Documentation

This `docs/` folder contains API notes, ERD and run instructions.

- API endpoints: `/api/` (DRF router). See backend `core/urls.py` for available routes.
- ERD: `ERD.md` (placeholder)
